name: bytebrain
version: 1.0.0+1
description: >-
  A learning app with subjects, lessons, quizzes, and AI Q&A.
environment:
  sdk: ">=2.17.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.2.2
  shared_preferences: ^2.2.2
  crypto: ^3.0.3
  flutter_dotenv: ^5.1.0
  url_launcher: ^6.2.1
  audioplayers: ^5.2.1
  video_player: ^2.8.1
  socket_io_client: ^2.0.3+1
  google_fonts: ^6.1.0
  flutter_markdown: ^0.6.18
  markdown: ^7.1.1
  # flutter_tts: ^3.8.5  # Disabled for Windows compatibility
  # Add other dependencies as needed

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/fonts/
    - assets/data/
    - assets/avatars/
    - .env
